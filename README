HOW TO RUN

1. Download Node.js https://nodejs.org/en/download/
2. open terminal check installation ('node -v') and ('npm -v')

---

3. Clone Repo through Github desktop
4. Open a terminal on that folder containing (cd 'foldername')
5. run ('npm install)
6. run program ('npm start')
